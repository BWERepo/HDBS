.ftp-credentials
secrets.php
secrets.staging.php
debug.flag

# ── Cloudflare / Workers migration ──
node_modules/
.wrangler/
.output/
dist/
.dev.vars
deploy-staging.log
deploy-prod.log

# Local mirror of the admin-uploaded media that exists ONLY on the Hostinger server.
# Large and binary; the permanent copy belongs in a backup location, not in git.
media-mirror/

# public/ is the Workers Static Assets directory. index.html is a real tracked source file
# (derived from index.php in Phase 2); everything else under it is generated by
# scripts/sync-assets.mjs from the authoritative copies at the repo root.
public/js/
public/css/
public/.well-known/
public/favicon.png
public/robots.txt
public/sitemap.xml
public/HDBSLogo.jpeg
public/hero.jpg
public/aboutsuzi.jpeg
public/QRCode.png
